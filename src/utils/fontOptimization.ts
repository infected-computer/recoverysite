// Font optimization is now handled via local CSS import.

export const initializeFontOptimization = (): void => {
  // This function is now empty as font loading is managed by src/styles/fonts.css
};
